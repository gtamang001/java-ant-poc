name: Ant CI sample 
on: push 
jobs: 
  ant-build: 
    runs-on: ubuntu-latest
    container: frekele/ant:latest
    steps: 
    - name: checkout 
      uses: actions/checkout@v2
    - name: ant build 
      run: |
        ant compile 
        ant jar 
        ant run  